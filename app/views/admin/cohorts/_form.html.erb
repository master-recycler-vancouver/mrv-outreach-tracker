<%= form_for [:admin, @cohort] do |form| %>
  <div class="field">
    <%= form.label :name, class: "label" %>
    <div class="control">
      <%= form.text_field :name, class: "input" %>
    </div>
  </div>

  <div class="control">
    <%= form.submit class: "button is-primary" %>
  </div>
<% end %>